# Build plan: ADR-0012 — CI shape (roadmap item 10)

> Generated by the `adr-plan` workflow (2026-07-25, run `wf_041c273e-79b`). 11 slices,
> 5 phases — **all Opus**: this is CI wiring and procedure design, not geometry; no
> Fable sitting anywhere. Check slices off as they land; move to
> `doc/plans/completed/` when done.
>
> The batching principle is different from every prior plan: **batch by tag-push
> cost, not just by dependency.** release.yml is only truly testable by pushing a
> tag against a Windows runner, so phases 2 and 3 each land as one commit + one
> test tag rather than per-slice pushes. The ADR itself predicts first-tag
> iteration; budget 2–4 red runs per phase and don't read them as defeat.
>
> ## Maintainer adjudications
> 1. **The two adversarial verifies stand** (`wasm-substitution-check-windows`,
>    `asar-integrity-off-check-windows`) — both are *compliance negatives* where a
>    vacuous check stays green forever: exactly the silent-failure class that has
>    earned every verify pass in this repo. The verify shape is specified per-slice
>    below; "the test passes" proves nothing for a negative test.
> 2. **Test tags vs the version gate collide** — the synthesizer suggests
>    `v0.0.x-test` throwaway tags, but the version-match gate would fail them
>    unless package.json is bumped each time. Resolution: release.yml also gets a
>    `workflow_dispatch` trigger that skips the gate and the draft-release step
>    (build + checks only). Iteration happens via dispatch; tags stay honest.
>    This is a plan-level mechanism, not an ADR change — the tag contract is
>    untouched.
> 3. **No cross-env** — `e2e:packaged`'s POSIX env prefix breaks on windows-latest,
>    and the obvious npm fix is a new dependency. CLAUDE.md's rule (any runtime
>    dep needs an ADR) doesn't bind devDeps, but the cheaper fix is zero deps:
>    `shell: bash` on the workflow step (bash ships on windows runners) or an env
>    block. Do that.

## Phases (dependency-ordered, batched by tag-push cost)

### 1. Foundations — the two workflow files · **opus batch**
- [ ] `ci-verify-workflow` · medium — `.github/workflows/ci.yml`, one ubuntu job on
      push/PR: `npm ci` → typecheck → vitest → `npm run package` →
      `xvfb-run npm run e2e:packaged`. Playwright traces uploaded on failure only.
      First-ever CI for this repo: xvfb + SwiftShader unknowns fail loudly;
      iterate to green.
- [ ] `release-windows-leg` · medium — `.github/workflows/release.yml` on `v*` +
      `workflow_dispatch` (adjudication 2): windows-latest, `npm ci` → build →
      `electron-builder --win nsis zip`. The yml's `win.target` lists only zip;
      the CLI override supplies nsis — deliberate (ADR-0010), don't "fix" the yml.
      Independent of ci.yml — don't let xvfb debugging block starting this.

### 2. release.yml fan-out — gates, second leg, Windows smoke · **opus batch, one tag/dispatch**
- [ ] `version-match-gate` · low — early step: strip `v` from the tag, compare to
      package.json version, die on mismatch; build legs depend on it. Skipped
      under `workflow_dispatch`.
- [ ] `release-linux-appimage-leg` · low — ubuntu leg, `--linux AppImage` via CLI
      override (the yml's `linux.target` is only `dir` — the ADR's "config exists"
      phrasing overstates; one-line override or yml addition).
- [ ] `windows-e2e-packaged` · medium — run the full e2e suite against
      `win-unpacked` on the Windows leg. Harness already takes `PACKAGED_APP` and
      uses `path.join` + `setInputFiles`; the real work is the npm script's POSIX
      env prefix (adjudication 3) and whatever windows-latest actually breaks.
- [ ] `asar-integrity-off-check-windows` · medium · **verify** — assert enforcement
      is OFF on the built Windows app so the LGPL substitution right stays real
      (ADR-0011). Verify shape: the check must FAIL against a build with
      integrity enforced (or a tampered-asar launch must demonstrably not be
      rejected) — a config grep or an untampered launch is the vacuous version.

### 3. Compliance + draft release · **opus batch, one tag/dispatch**
- [ ] `wasm-substitution-check-windows` · medium · **verify** — automate ADR-0011's
      manual test in the Windows leg: overwrite the **asarUnpack'd** wasm in
      `win-unpacked` with junk, launch via the packaged harness, import a
      `samples/` STEP, assert *failure*; restore. Verify shape: (a) confirm the
      test FAILS (import succeeds) when the wasm is left intact, (b) confirm the
      replaced copy is the loaded one — app.asar carries a second, unused wasm
      (roadmap item 9 carry-in), and replacing that one is the trap.
      Discharges roadmap item 10's ADR-0011 carry-in.
- [ ] `draft-release-upload` · medium — both legs' artifacts converge on ONE
      **draft** release for the tag (create-if-missing/append semantics;
      `contents: write`). Eyeball `draft: true` before the tag push — the silent
      failure here is auto-publishing an undogfooded build. Skipped under
      `workflow_dispatch`.

### 4. Consumption + optimization · **opus batch**
- [ ] `deploy-fetch-path` · medium — amend `.claude/skills/deploy/SKILL.md` step 1:
      when the current sha has a completed release build, `gh release download`
      into `dist-live/` instead of building; local zip build stays the fallback.
      Staging/rollback/BUILD_SHA/dogfood semantics unchanged. Resolve sha→tag
      explicitly; "CI has run for this sha" must be a check, not an assumption.
- [ ] `ci-npm-cache` · low — `cache: npm` on setup-node in both workflows, after
      they're stable green so a cache regression is attributable.

### 5. Docs reconciliation · **opus**
- [ ] `docs-reconcile-ci` · low — check off roadmap item 10; mark the ADR-0011
      Windows carry-in discharged; delete CLAUDE.md's "xvfb installed nowhere
      yet" and update the zip-until-CI phrasing in `/deploy`'s description;
      VISION's target-platform section likely already reads true — confirm.

## Critical path

`release-windows-leg` → `windows-e2e-packaged` → `wasm-substitution-check-windows`
→ `draft-release-upload` → `deploy-fetch-path` → `docs-reconcile-ci`

## Sequencing risks (from the synthesizer, kept verbatim where sharp)

- Phase 1's two workflows are independent — don't serialize them.
- `draft: true` + permissions are the one silent-failure config in phase 3.
- The AppImage leg needs the CLI override; don't trust "config exists".
- Fix the e2e:packaged env-prefix in phase 2 *before* phase 3 reuses the plumbing.
- **No macOS leg** — explicit ADR non-goal; don't add one while touching the matrix.
